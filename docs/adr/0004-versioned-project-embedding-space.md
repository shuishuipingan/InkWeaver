# 版本化项目嵌入空间

每个项目同一时刻只激活一个有版本的嵌入空间，空间身份包含模型指纹、向量维度、比较语义和索引代际；同一向量集合不得混入不同空间的数据。写入前必须验证批次数量、统一维度和全部有限数值，首次向量按实际维度创建 schema。模型或维度变化时不得追加、删除旧表或静默转换，而是保留旧代际、维持全文检索并标记需要重建；新代际完成验证后才能原子切换为 active，避免跨模型语义污染和用户数据损坏。
