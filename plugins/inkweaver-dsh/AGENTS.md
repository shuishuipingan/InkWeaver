# InkWeaver DSH Plugin Agent Rules

## V2 development gate

Before changing or qualifying this plugin, read [docs/v2-development-gates.md](docs/v2-development-gates.md). It owns the project-specific V2 runtime gates, including the packaged-preset/profile boundary and the keyless browser evidence required before calling a change usable.

## Subagent model routing

When spawning subagents for this plugin, use the native gpt-5.6-terra model with xhigh reasoning effort.
