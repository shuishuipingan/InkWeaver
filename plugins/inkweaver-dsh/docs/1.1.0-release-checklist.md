# InkWeaver DSH 1.1.0 分发清单

状态：已随 GitHub Release `v1.1.0` 完成发布回读。  
适用包：`@shuishuipingan/inkweaver-dsh`  
目标版本：`1.1.0`  
官方宿主兼容基线：`@deepseek-ai/dsh 0.1.5-rc.1`

## 发布范围决议（2026-09-12）

本轮发布范围明确：**不发布 npm**。插件以 GitHub Release tarball、本地 `dsh plugin add <tarball>` 安装说明和桌面安装包随版本交付；不得执行 `pnpm publish`，也不得把 registry 404 解读成失败。npm 权限审计只保留为历史记录，不是本轮发布阻塞。

真实外部双评阅者和 provider 质量盲评也由发布负责人豁免。质量收据改用固定 fixture、自动化测试、browser suite、provider dry-run 和可审计的评分模板；这些证据不能宣称真实模型质量已通过，只说明已按豁免范围完成工程门禁。

## 冻结前提

以下条件曾作为 1.1.0 冻结门槛，现已全部满足：

1. 1.1.0 追踪表的功能需求有工程证据，12 条端到端旅程在冻结 SHA 上重跑；质量外部评阅按上面的发布范围决议豁免并留档；
2. DSH qualification 在冻结源码 SHA 上重新执行并生成 `passed` receipt；
3. `package.json` 与 `plugins/inkweaver-dsh/package.json` 同时冻结到 `1.1.0`；
4. README、兼容矩阵、CHANGELOG 和 GitHub 分发收据都指向同一源码 SHA；
5. GitHub Release tarball、包名、topic 元数据和本地安装路径已经由发布负责人确认；npm publish 不属于本轮范围。

桌面与插件 package version 已同步到 `1.1.0`；Windows/macOS 资产、J12、插件 tarball
和 GitHub Release 回读均已完成。npm 不属于本轮发布范围。

## 冻结后的执行顺序

在仓库根目录：

```text
node scripts/release-version-sync.mjs --expected-version 1.1.0
pnpm run typecheck
pnpm test -- --maxWorkers=1
```

在插件目录：

```text
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm run build
pnpm pack --pack-destination .runtime/release-artifacts
```

然后使用与源码 SHA 绑定的 Harness checkout 执行完整 qualification：

```text
node plugins/inkweaver-dsh/scripts/qualify-release.mjs \
  --harness-root <clean-official-harness-checkout>
```

qualification 必须回读并保存：

- tarball 文件名、字节数和 SHA-256；
- 包名、版本、preset/Host 身份和 41 项包内容；
- profile add/remove/reinstall 结果；
- `inkweaver-v2` roster/mount 和 exactly-two-tool 清单；
- Proposal 应用、重启、重装和 schema 5 持久化读回；
- 三次真实 Chrome Web journey 与截图/几何数据；
- 官方 Harness commit、插件源码 commit 和锁文件哈希。

## GitHub tarball 与主题发布

qualification receipt 通过后，将冻结 tarball SHA 并把 tarball 作为 GitHub Release 资产；安装说明使用本地 tarball 路径，不执行 npm publish。历史包名 `@ethanyoq/dsh-ai-novel-writer` 不得重新出现在 manifest、README 或发布命令中。

GitHub Release 发布后必须回读资产文件名、字节数、SHA-256、preset/mount receipt、topic 元数据和本地 tarball 安装路径，并把同一 SHA 写入 `docs/upgrade/DSH-MANUAL-PROFILE-RECEIPT.md` 和 `docs/upgrade/GITHUB-DISTRIBUTION-RECEIPT.md`。

`@linxin666/dsh-web-all@0.3.20` 只是外部 DSH Web 宿主依赖，不得打入
InkWeaver tarball，也不得被描述为本项目插件。

GitHub `dsh-plugin` topic 的元数据和实际主题页索引是两项独立证据。正式
Release 创建后必须重新回读仓库 topics、Release 资产和主题页；索引延迟时
应在 CHANGELOG/分发收据中明确记录，不得宣称“已可见”。

## 回滚

若 qualification receipt、GitHub Release 资产、tarball 安装或 topic 回读
任何一项不一致，停止公开发布，保留当前开发分支和 receipt，撤回未完成的
Release 草稿；不要通过重新打包或改写哈希来掩盖不一致。npm registry 仍应保持未发布状态。
